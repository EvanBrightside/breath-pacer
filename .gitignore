node_modules/

# Build output: regenerated by `zeus build` and named per build timestamp.
dist/

# Encodes a live install link tied to the developer's Zepp account.
# Regenerate locally with `zeus preview`; never commit.
preview-qr.png
